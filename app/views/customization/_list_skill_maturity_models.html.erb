<h1>Listing skill maturity models</h1>

<table cellspacing="0" cellpadding="0">
  <colgroup>
    <col width="200">
    <col width="200">
  </colgroup>
  <tr>
    <th class="header">Name</th>
    <th class="header">Description</th>
  </tr>

  <% for e in SkillMaturityModel.find(:all) %>
    <tr class="<%=cycle('record', 'even-record record') %>">
      <td><%=link_to e.name, :action => "edit_skill_maturity_model", :id => e %></td>
      <td><%=h e.description %></td>
    </tr>
  <% end %>

</table>
